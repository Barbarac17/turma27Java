package lista04_vetor_matriz;

import java.util.Random;
import java.util.Scanner;

/*Crie um programa que receba valores do usuário para preencher uma matriz 3X3, e em seguida, exiba a soma dos valores dela e a soma dos valores da primeira diagonal, 
 * ou seja, diagonal principal.
 */

public class lista4ex04 {

	public static void main(String[] args) {
		Scanner read = new Scanner(System.in);
		Random gerador = new Random();
				

		int matriz [][] = new int [3][3];

		int linha = 0, somaL=0;
		int coluna = 0;
		int diagonal = 0;
				
		for (linha = 0; linha < 3; linha ++){
			for(coluna = 0; coluna < 3; coluna ++){
				/*matriz [linha][coluna] = gerador.nextInt(2,4);
				
				escreva(matriz [linha][coluna], " ")
				somaL += matriz [linha][coluna]
				
				se(linha == coluna){
					diagonal += matriz [linha][coluna]*/
					
				}
		

	}

}
